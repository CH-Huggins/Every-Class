<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Courses</title>
    <link rel="stylesheet" href="/css/courses.css">
    <link rel="stylesheet" href="/css/nav.css">
    <script src="/js/nav.js" defer></script>
</head>
<body>
    <div id="nav">
        <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">&times;</a>
        <a href="/api/home">Home</a>
        <a href="/api/courses">Courses</a>
        <a href="/api/messages">Messages</a>
        <a href="/api/profile">Profile</a>
        <a id="logout" href="/api/logout">Log Out</a>
    </div>
    <span style="font-size:50px;cursor:pointer;color:white;" onclick="openNav()">&#9776;</span>
    
    <div class="wrapper">
        <button onclick="window.location.href = '/api/addCourse'">+</button>
        <% if (courses.length == 0) { %>
            <p id="noClass"> You have no courses. Try adding some with the + button.</p>
        <% } else { %>
            <% if (!added) { %>
                <div class="course" style="color: red;">
                    <div class="courseDivs" style="margin-left: calc(40px + 10%);">
                        <p class="details">CRN:</p>
                        <p class="courseDetails">
                            -
                        </p>
                    </div>
                    <div class="courseDivs">
                        <p class="details">Course:</p>
                        <p class="courseDetails">
                        Course could not be added, MAX HOURS
                        </p>
                    </div>
                    <div class="courseDivs">
                        <p class="details">Credits:</p>
                        <p class="courseDetails">
                            -
                        </p>
                    </div>
                </div>
            <% } %>
            <% for (let i = 0; i < courses.length; i++) { %>
                <a href="/api/course/<%= courses[i].CourseName %>">
                    <div class="course">
                        <form id="dropCourse" action="/api/droppedCourse" method="POST">
                            <input type="hidden" name="CRN" value="<%= courses[i].CRN %>">
                            <button type="submit" class="submit" value="Submit">-</button>
                        </form>
                        <div class="courseDivs">
                            <p class="details">CRN:</p>
                            <p class="courseDetails">
                            <%= courses[i].CRN %>
                            </p>
                        </div>
                        <div class="courseDivs">
                            <p class="details">Course:</p>
                            <p class="courseDetails">
                            <%= courses[i].CourseName %>
                            </p>
                        </div>
                        <div class="courseDivs">
                            <p class="details">Credits:</p>
                            <p class="courseDetails">
                                <%= courses[i].credits %>
                            </p>
                        </div>
                    </div>
                </a>
            <% } %>
        <% } %>
    </div>
</body>
</html>