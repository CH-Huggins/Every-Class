<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>New Conversation</title>
    <link rel="stylesheet" href="/css/newConversation.css">
    <link rel="stylesheet" href="/css/nav.css">
    <script src="/js/nav.js" defer></script>
</head>
<body>
    <div id="nav">
        <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">&times;</a>
        <a href="/api/home">Home</a>
        <a href="/api/courses">Courses</a>
        <a href="/api/messages">Messages</a>
        <a href="/api/profile">Profile</a>
        <a id="logout" href="/api/logout">Log Out</a>
    </div>
    <span style="font-size:50px;cursor:pointer;color:white;" onclick="openNav()">&#9776;</span>

    <div class="wrapper">
        <h1>Find a user:</h1>
        <p class="description">(Typing their name should immediately find it)</p>
        <form id="messagesAdded" action="/api/messages/added" method="POST">
            <label for="users"><b>Users:</b></label>
            <select name="userID" id="userID">
                <% for (let i = 0; i < users.length; i++){ %>
                    <option value="<%= users[i].userID %>">
                        <%= users[i].firstName %>
                        <%= users[i].lastName %>
                    </option>
                <% } %>
            </select>
            <button type="submit" class="submit" value="Submit">Add User</button>
        </form>
    </div>
</body>
</html>